UmlCanvas.Role = Class.create({});

UmlCanvas.Role.from = function(construct, diagram) {
    return null;
};

UmlCanvas.Role.MANIFEST = {
    name : "role"
}
    
Canvas2D.registerShape(UmlCanvas.Role);
